# 架构说明

> 本技能设计为跨平台 Agent Skill，已上架 [SkillsMP](https://skillsmp.com) 和 [腾讯 SkillHub](https://skillhub.cn)。
> 遵循 [Agent Skills](https://agentskills.io/) 开放标准，兼容所有支持该标准的 AI 工具。

## 整体架构

```
┌─────────────────────────────────────────────────┐
│                  用户输入                          │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│              SKILL.md (入口)                      │
│   - 路由到对应功能                                │
│   - 提供操作流程                                  │
│   - 引用资源文件                                  │
└────┬──────┬──────┬──────┬──────┬───────────────┘
     │      │      │      │      │
     ▼      ▼      ▼      ▼      ▼
┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌──────────┐
│症状  │ │药物  │ │文献  │ │术语  │ │报告解读  │
│分析  │ │查询  │ │检索  │ │解释  │ │          │
└──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘ └────┬─────┘
   │       │       │       │         │
   ▼       ▼       ▼       ▼         ▼
┌─────┐ ┌─────┐ ┌─────┐ ┌─────────┐ ┌─────┐
│Script│ │Script│ │Script│ │Reference│ │Script│
└──┬──┘ └──┬──┘ └──┬──┘ └─────────┘ └──┬──┘
   │       │       │                    │
   ▼       ▼       ▼                    ▼
┌─────┐ ┌─────┐ ┌──────┐          ┌────────┐
│Open │ │Open │ │PubMed│          │AI 模型 │
│FDA  │ │FDA  │ │API   │          │(回退)  │
└─────┘ └─────┘ └──────┘          └────────┘
```

## 分层设计

### 1. 入口层 (SKILL.md)
- 负责路由用户请求到对应功能
- 提供清晰的流程指引
- 引用脚本和参考资源

### 2. 脚本层 (scripts/)
- Python 编写的 API 调用脚本
- 处理外部 API 通信
- 数据格式化和错误处理
- 设计为"回退安全"——API 失败时使用 AI 知识

### 3. 知识层 (references/)
- 静态 Markdown 知识文档
- 由 AI 在上下文中读取
- 作为 API 信息的补充

### 4. 模板层 (assets/templates/)
- 输出格式模板
- 确保输出的一致性和专业性

## 设计原则

1. **渐进式加载**：SKILL.md < 500 行，详细信息放在参考文献中
2. **API 优先，AI 回退**：优先使用权威 API 数据，API 不可用时回退到 AI 模型知识
3. **免责优先**：所有输出必须包含免责声明
4. **多平台兼容**：遵循 Agent Skills 开放标准，已上架 SkillsMP 和腾讯 SkillHub，兼容 Claude Code、OpenAI Codex CLI、Cursor、VS Code 等所有支持该标准的工具
